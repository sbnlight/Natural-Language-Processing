{
  "url": "https://wiki.eecs.berkeley.edu/accessors/Version1/Parameter",
  "chunks": [
    {
      "chunk_id": 1,
      "text": "Accessors Wiki | Version1 / Parameter Recent Changes - Search : iCyPhy Home Accessors Home Accessors Wiki Editing Instructions All Recent Changes edit SideBar View Edit History Print Version1 / Parameter An accessor may specify parameters in the body"
    },
    {
      "chunk_id": 2,
      "text": "ter An accessor may specify parameters in the body of the setup() function by invoking: parameter ( <string> name , [ options ]): Create the parameter. Options are optional. A parameter, unlike an input, is not expected to change value during the exe"
    },
    {
      "chunk_id": 3,
      "text": "ut, is not expected to change value during the execution of a swarmlet. NOTE : Perhaps it should be required that whenever a parameter value is changed, the setup() function is re-executed. This could be useful, for example, if the possible values of"
    },
    {
      "chunk_id": 4,
      "text": "be useful, for example, if the possible values of one parameter depend on the value of another parameter. For example, the following accessor will send the value of the foo parameter to its output bar once: exports. setup = function ( ) { this . par"
    },
    {
      "chunk_id": 5,
      "text": "r once: exports. setup = function ( ) { this . parameter ( 'foo' , { 'value' : 'y' , 'type' : 'string' } ) ; this . output ( 'bar' ) ; } exports. initialize = function ( ) { this . send ( 'bar' , this . getParameter ( 'foo' ) ) ; } [$[Get Code]] The"
    },
    {
      "chunk_id": 6,
      "text": ". getParameter ( 'foo' ) ) ; } [$[Get Code]] The parameter () function takes one or two arguments, a name (a required string, recommended to be camelCase with a leading lower-case character) and an object with any of the following options: type The"
    },
    {
      "chunk_id": 7,
      "text": "bject with any of the following options: type The data type of the parameter. If this is not specified, then any valid JavaScript value may be provided as a value. If it is specified, it must be one of the valid data types . value The default value f"
    },
    {
      "chunk_id": 8,
      "text": "f the valid data types . value The default value for the parameter. Specifying a default value ensures that getParameter () will not return null (see below). options An array of possible values for this parameter. A user interface may list these poss"
    },
    {
      "chunk_id": 9,
      "text": "is parameter. A user interface may list these possible values, and a host may restrict the values to be one of these values. visibility A hint to the host of whether this parameter should be made visible to casual users. This is a string that is one"
    },
    {
      "chunk_id": 10,
      "text": "ble to casual users. This is a string that is one of \"full\", \"none\", \"expert\", or \"notEditable\", meaning \"full visibility\", \"no visibility\", \"expert visibility\", or \"visible, but not editable\". This option is useful for accessors that extend other ac"
    },
    {
      "chunk_id": 11,
      "text": "ption is useful for accessors that extend other accessors but do not want to make the parameters of the base accessor visible in the derived accessor. The name is required to be distinct from other inputs, outputs, and parameters. It may match the na"
    },
    {
      "chunk_id": 12,
      "text": "puts, outputs, and parameters. It may match the name of a parameter in a base accessor (specified by either extend() or implement()), in which case the options will override those specified in the base. Getting Parameters The current value of a param"
    },
    {
      "chunk_id": 13,
      "text": "e. Getting Parameters The current value of a parameter may be retrieved in the accessor's JavaScript code using the getParameter () function, as in the following example: var address = this . getParameter ( 'bridgeIPAddress' ) ; [$[Get Code]] The get"
    },
    {
      "chunk_id": 14,
      "text": "eter ( 'bridgeIPAddress' ) ; [$[Get Code]] The getParameter () function takes one argument, the parameter name (as a string). If no value option is given, then getParameter () will return null if the host has not provided a parameter value. Setting P"
    },
    {
      "chunk_id": 15,
      "text": "host has not provided a parameter value. Setting Parameters The current value of a parameter may be set in the accessor's JavaScript code using the setParameter () function, as in the following example: this . setParameter ( 'bridgeIPAddress' , '128."
    },
    {
      "chunk_id": 16,
      "text": "e: this . setParameter ( 'bridgeIPAddress' , '128.32.12.0' ) ; [$[Get Code]] This might be done, for example, in an accessor that extends another to change the default value of the parameter. See Also Input Output Back to accessor specification Edit"
    }
  ]
}