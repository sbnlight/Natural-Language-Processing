{
  "url": "http://inst.eecs.berkeley.edu/share/b/pub/grading/bin/get-subm",
  "chunks": [
    {
      "chunk_id": 3,
      "text": "does not exist.\"); } if ($patn =~ /\\./) { if (! -e \"$SUBMISSION_DIR/$assgn/$patn\") { Fatal (\"Cannot find submission $patn for assignment $assgn.\"); } $file = \"$SUBMISSION_DIR/$assgn/$patn\"; } else { if (@allEntries = GLOB (\"$SUBMIS"
    },
    {
      "chunk_id": 4,
      "text": "tn\"; } else { if (@allEntries = GLOB (\"$SUBMISSION_DIR/$assgn/$patn.*\")) { @allEntries = sort SubmissionOrder @allEntries; $file = pop (@allEntries); } else { Fatal (\"No entries by $patn for assignment $assgn.\"); } } ($base) ="
    },
    {
      "chunk_id": 5,
      "text": "patn for assignment $assgn.\"); } } ($base) = ($file =~ m\" ([^/]+)$ \"x); Note (\"Unpacking submission $base for assignment $assgn\\n\" . \" into current directory.\"); UnpackSubmission ($assgn, $file, \".\", 1) || Fatal (\"Could not unpack"
    },
    {
      "chunk_id": 6,
      "text": "n, $file, \".\", 1) || Fatal (\"Could not unpack submission.\");"
    }
  ]
}