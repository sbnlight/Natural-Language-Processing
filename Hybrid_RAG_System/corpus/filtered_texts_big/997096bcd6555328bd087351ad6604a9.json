{
  "url": "http://ptolemy.eecs.berkeley.edu/ptolemyII/ptII3.0/ptII3.0.2/doc/codeDoc/ptolemy/actor/lib/package-summary.html",
  "chunks": [
    {
      "chunk_id": 2,
      "text": "terface for actors that operate on sequences. TimedActor This is a marker interface for actors that operate on time-based signals. Class Summary AbsoluteValue Produce an output token on each firing with a value that is equal to the absolute value of"
    },
    {
      "chunk_id": 3,
      "text": "th a value that is equal to the absolute value of the input. Accumulator Output the initial value plus the sum of all the inputs since the last time a true token was received at the reset port. AddSubtract A polymorphic adder/subtractor. ArrayAppend"
    },
    {
      "chunk_id": 4,
      "text": "tract A polymorphic adder/subtractor. ArrayAppend An actor that appends ArrayTokens together. ArrayElement Extract an element from an array. ArrayExtract Extract a subarray from an array. ArrayLength Output the length of an array. ArrayMaximum Extrac"
    },
    {
      "chunk_id": 5,
      "text": "Output the length of an array. ArrayMaximum Extract the maximum element from an array. ArrayMinimum Extract the minimum element from an array. Average Output the average of the inputs after the last time a true token is received at the reset port. Be"
    },
    {
      "chunk_id": 6,
      "text": "ime a true token is received at the reset port. Bernoulli Produce a random sequence of booleans. BooleanMultiplexor A type polymorphic multiplexor with boolean valued select. BooleanSelect A type polymorphic select with boolean valued control. Boolea"
    },
    {
      "chunk_id": 7,
      "text": "morphic select with boolean valued control. BooleanSwitch A type polymorphic switch with boolean valued control. BusAssembler Aggregate all input relation channels into one output \"bus\" relation. BusDisassembler Split the input bus relation into indi"
    },
    {
      "chunk_id": 8,
      "text": "isassembler Split the input bus relation into individual (possibly bus) output port relations. ClassWrapper This actor wraps (an instance of) a Java class specified by the className parameter. Clock This actor produces a periodic signal, a generalize"
    },
    {
      "chunk_id": 9,
      "text": "his actor produces a periodic signal, a generalized square wave that sequences through N output values with arbitrary duty cycles and period. Commutator A polymorphic commutator, which merges a set of input sequences into a single output sequen"
    },
    {
      "chunk_id": 10,
      "text": "set of input sequences into a single output sequence. Const Produce a constant output. Counter This actor implements an up-down counter of received tokens. CurrentTime Produce an output token on each firing with a value that is the current time. DB P"
    },
    {
      "chunk_id": 11,
      "text": "firing with a value that is the current time. DB Produce a token that is the value of the input in decibels. Differential Output the current input minus the previous input, or if there has been no previous input, the current input itself. Discard A s"
    },
    {
      "chunk_id": 12,
      "text": "vious input, the current input itself. Discard A simple sink actor that consumes and discards input tokens. DiscreteRandomSource An actor that produces tokens with a given probability mass function. Distributor A polymorphic distributor, which splits"
    },
    {
      "chunk_id": 13,
      "text": "istributor A polymorphic distributor, which splits an input stream into a set of output streams. DoubleReader Deprecated. Use ExpressionReader instead. Expression On each firing, evaluate an expression that may include references to the inputs, cu"
    },
    {
      "chunk_id": 14,
      "text": "n that may include references to the inputs, current time, and a count of the firing. FileWriter Deprecated. This actor is deprecated. Gaussian Produce a random sequence with a Gaussian distribution. IIR This actor is an implementation of an infin"
    },
    {
      "chunk_id": 15,
      "text": "n. IIR This actor is an implementation of an infinite impulse response IIR filter. Interpolator Produce an interpolation based on the parameters. Lattice An FIR filter with a lattice structure. LevinsonDurbin This actor uses the Levinson-Durbin algor"
    },
    {
      "chunk_id": 16,
      "text": "onDurbin This actor uses the Levinson-Durbin algorithm to compute the linear predictor coefficients of a random process, given its autocorrelation function as an input. Limiter Produce an output token on each firing with a value that is equal to the"
    },
    {
      "chunk_id": 17,
      "text": "on each firing with a value that is equal to the input if the input lies between the bottom and top parameters. LinearDifferenceEquationSystem The linear state-space model implements a system whose behavior is defined by: x(k+1) = Ax(k) + Bu(k)"
    },
    {
      "chunk_id": 18,
      "text": "ehavior is defined by: x(k+1) = Ax(k) + Bu(k) y(k) = Cx(k) + Du(k) x(0) = x0 where x is the state vector, u is the input vector, and y is the output vector. LookupTable Output to the output port the value in the array of tokens spe"
    },
    {
      "chunk_id": 19,
      "text": "e output port the value in the array of tokens specified by the table parameter at the index specified by the input port. MathFunction Produce an output token on each firing with a value that is equal to the specified math function of the input. Maxi"
    },
    {
      "chunk_id": 20,
      "text": "to the specified math function of the input. Maximum Read at most one token from each input channel and broadcast the one with the greatest value to the maximumValue output. MaxIndex Deprecated. Use Maximum instead. Minimum Read at most one token fr"
    },
    {
      "chunk_id": 21,
      "text": "Maximum instead. Minimum Read at most one token from each input channel and broadcast the one with the least value to the minimumValue output. MonitorValue Monitor inputs by setting the value parameter equal to each arriving token. Multiplexor This a"
    },
    {
      "chunk_id": 22,
      "text": "r equal to each arriving token. Multiplexor This actor selects from the channels on the input port, copying the input from one channel to the output, based on the most recently received value on the select input. MultiplyDivide A polymorphic multipli"
    },
    {
      "chunk_id": 23,
      "text": "elect input. MultiplyDivide A polymorphic multiplier and/or divider. NonStrictTest This actor compares the inputs against the value specified by the correctValues parameter. OrderedMerge This actor merges two monotonically nondecreasing streams of to"
    },
    {
      "chunk_id": 24,
      "text": "rges two monotonically nondecreasing streams of tokens into one monotonically nondecreasing stream. PhaseUnwrap This actor unwraps a phase plot, removing discontinuities of magnitude 2*PI. PoissonClock This actor produces a signal that is piecewise c"
    },
    {
      "chunk_id": 25,
      "text": "k This actor produces a signal that is piecewise constant, with transitions between levels taken at times given by a Poisson process. Pulse Produce a pulse with a shape specified by the parameters. Quantizer Produce an output token on each firing wit"
    },
    {
      "chunk_id": 26,
      "text": "antizer Produce an output token on each firing with a value that is a quantized version of the input. Ramp Produce an output token on each firing with a value that is incremented by the specified step each iteration. RandomSource A base class for sou"
    },
    {
      "chunk_id": 27,
      "text": "each iteration. RandomSource A base class for sources of random numbers. Reader This actor reads tokens from an URL, and output them. RecordAssembler On each firing, read one token from each input port and assemble them into a RecordToken. RecordDis"
    },
    {
      "chunk_id": 28,
      "text": "rt and assemble them into a RecordToken. RecordDisassembler On each firing, read one RecordToken from the input port and send out the fields of the RecordToken to multiple output ports. Recorder Record all input tokens for later querying. RecordUpdat"
    },
    {
      "chunk_id": 29,
      "text": "d all input tokens for later querying. RecordUpdater On each firing, read one token from each input port and assemble them into a RecordToken that contains the union of the original input record and each of the update ports. RecursiveLattice A recurs"
    },
    {
      "chunk_id": 30,
      "text": "ach of the update ports. RecursiveLattice A recursive (all-pole) filter with a lattice structure. Remainder Compute the remainder after dividing the input by the divisor. Rician Produce a random sequence with a Rician distribution. Scale Produce an o"
    },
    {
      "chunk_id": 31,
      "text": "nce with a Rician distribution. Scale Produce an output token on each firing with a value that is equal to a scaled version of the input. Select A polymorphic select, which routes specified input channels to the output. Sequencer This actor takes a s"
    },
    {
      "chunk_id": 32,
      "text": "nels to the output. Sequencer This actor takes a sequence of inputs tagged with a sequence number and produces them on the output port in the order given by the sequence number. SequenceSource Base class for sequence sources. SequentialClock Deprecat"
    },
    {
      "chunk_id": 33,
      "text": "ass for sequence sources. SequentialClock Deprecated. Use Clock instead. Sink Base class for simple data sinks. Sleep An actor that calls Thread.sleep() on the current thread the first time fire() is called. Source Base class for simple data sources."
    },
    {
      "chunk_id": 34,
      "text": "called. Source Base class for simple data sources. Stop An actor that stops execution of a model when it receives a true token on any input channel. Switch A polymorphic switch, which routes inputs to specified output channels. Synchronizer This acto"
    },
    {
      "chunk_id": 35,
      "text": "specified output channels. Synchronizer This actor implements a token synchronizer. Test This actor compares the inputs against the value specified by the correctValues parameter. ThrowException An actor that throws an IllegalActionException when it"
    },
    {
      "chunk_id": 36,
      "text": "ctor that throws an IllegalActionException when it receives a true token on any input channel. ThrowModelError An actor that throws a model error when it receives a true token on any input channel. TimedSource Base class for time-based sources. Trans"
    },
    {
      "chunk_id": 37,
      "text": "medSource Base class for time-based sources. Transformer This is an abstract base class for actors that transform an input stream into an output stream. TrigFunction Produce an output token on each firing with a value that is equal to the specified t"
    },
    {
      "chunk_id": 38,
      "text": "ring with a value that is equal to the specified trigonometric function of the input. TriggeredClock This actor is an extension of Clock with a start and stop input. TypeTest An actor that can be used for regression test of the type resolution system"
    },
    {
      "chunk_id": 39,
      "text": "for regression test of the type resolution system. Uniform Produce a random sequence with a uniform distribution. URLDirectoryReader This actor reads a URL and if the URL names a directory, it outputs the name of each file or subdirectory contained"
    },
    {
      "chunk_id": 40,
      "text": "s the name of each file or subdirectory contained in the directory. URLReader This actor reads tokens from an URL, and output them. VariableClock This actor is identical to Clock except that it has an additional input port, periodControl . VariableSl"
    },
    {
      "chunk_id": 41,
      "text": "additional input port, periodControl . VariableSleep An actor that calls Thread.sleep() on the current thread the first time fire() is called. VectorAssembler On each firing, read exactly one token from each channel of the input port and assemble th"
    },
    {
      "chunk_id": 42,
      "text": "rom each channel of the input port and assemble the tokens into a DoubleMatrixToken with one column. VectorDisassembler On each firing, read one column vector (i.e. a DoubleMatrixToken with one column) from the input port and send out individual Doub"
    },
    {
      "chunk_id": 43,
      "text": ") from the input port and send out individual DoubleTokens to each channel of the output port. WallClockTime Upon firing, this actor outputs the elapsed real time in seconds since the invocation of its initialize() method. Writer This actor reads tok"
    },
    {
      "chunk_id": 44,
      "text": "s initialize() method. Writer This actor reads tokens from any number of input channels and writes their string values to the specified writer. Overview Package Class Tree Deprecated Index Help PREV PACKAGE NEXT PACKAGE FRAMES NO FRAMES All Classes"
    },
    {
      "chunk_id": 45,
      "text": "ACKAGE NEXT PACKAGE FRAMES NO FRAMES All Classes"
    }
  ]
}