{
  "url": "http://ptolemy.eecs.berkeley.edu/ptolemyII/ptII7.0/ptII7.0.1/ptolemy/codegen/demo/Ramp/Ramp.xml",
  "chunks": [
    {
      "chunk_id": 1,
      "text": "The code generator to run. The class that is instantiated to execute a particular code generator. This class is expected to be a subclass of ptolemy.copernicus.kernel.KernelMain. If true, compile the generated code. The default is true. If true, th"
    },
    {
      "chunk_id": 2,
      "text": "generated code. The default is true. If true, then show the generated code. The default is true. If true, then run the generated code. The default is true. The location of the Ptolemy II classes. The default is the value of the ptolemy.ptII.dir"
    },
    {
      "chunk_id": 3,
      "text": "The default is the value of the ptolemy.ptII.dir java system property The top level directory to write the code in. The default is the value of the ptII parameter. The code will appear in 'ptIIUserDirectory/targetPath'. The package to generate code"
    },
    {
      "chunk_id": 4,
      "text": "rectory/targetPath'. The package to generate code in. Defaults to the model name The path relative to the ptIIUserDirectory to generate code in. The directory that code will be generated in. The path to the model, including the .xml extension. model"
    },
    {
      "chunk_id": 5,
      "text": "to the model, including the .xml extension. modelPath is converted to a URL internally before use. The name of the model, read from the .xml file and sanitized. Number of iterations to run the model for. Usually, the model itself contains the number"
    },
    {
      "chunk_id": 6,
      "text": "for. Usually, the model itself contains the number of iterations to run for. Defaults to 1000. Defaults to \"iterations:\" + iterations. Set this to the empty string if the model has the number of iterations built in. User supplied arguments to be pas"
    },
    {
      "chunk_id": 7,
      "text": "ations built in. User supplied arguments to be passed to the code generator. Defaults to the empty string. The class path. Defaults to the value of of the java.class.path property. An array of Strings, where each element names a class that needs to"
    },
    {
      "chunk_id": 8,
      "text": "s, where each element names a class that needs to be present in the classpath Class path that refers to necessary classes. Derived from necessaryClasses The classpath separator. Under Windows, the default is ';'. Under Unix, the default is ':' Defaul"
    },
    {
      "chunk_id": 9,
      "text": "ault is ';'. Under Unix, the default is ':' Defaults to the value of the java.home property Defaults to the value of the java.home property + bin/java, for example: c:/jdk1.3.1/bin/java The flag to pass to the java process that defines the value of t"
    },
    {
      "chunk_id": 10,
      "text": "ss to the java process that defines the value of the ptolemy.ptII.dir property. Defaults to \"-Dptolemy.ptII.dir=\" + ptII Defaults to the value of the java.home property + \"/lib/rt.jar\" The command line arguments to pass to the java interpreter that"
    },
    {
      "chunk_id": 11,
      "text": "ne arguments to pass to the java interpreter that control the size. The default is -Xmx256m The directory for finding template files. The template file that contains the command to run the generated code. User supplied arguments to be passed to the"
    },
    {
      "chunk_id": 12,
      "text": "code. User supplied arguments to be passed to the command that will run the generated code. Defaults to the empty string. The directory that contains the soot jar files. Defaults to the value of the ptII parameter + \"/lib\" The location of sootclasse"
    },
    {
      "chunk_id": 13,
      "text": "tII parameter + \"/lib\" The location of sootclasses.jar, jasminclasses.jar and the Java system jar (usually rt.jar). The necessaryClassPath parameter may end up duplicating some of the elements of this parameter. The top level directory to write the"
    },
    {
      "chunk_id": 14,
      "text": "is parameter. The top level directory to write the code in as a URL. Derived from the value of the ptIIUserDirectory parameter. The number of milliseconds that code generation will run for. Defaults to 720000, which is 12 minutes. The filename to re"
    },
    {
      "chunk_id": 15,
      "text": "to 720000, which is 12 minutes. The filename to redirect the standard output stream of the code generator to. This is used, for example, in the nightly build to provide easily parseable error messages. If the value is not set, then the output will"
    },
    {
      "chunk_id": 16,
      "text": "s. If the value is not set, then the output will not be redirected. Create a sequence of tokens with increasing value SequencePlotter ?>"
    }
  ]
}